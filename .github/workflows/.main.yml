name: Main file

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  call-workflow:
    uses: exchange-rate-service/.github/workflows/test.yml@main
    with:
     secrets: inherit
